# if [ -n "$BASH_VERSION" ] && ! command -v ghcs &>/dev/null; then
#   # eval "$(gh copilot alias -- bash)"
# fi

# if [ -n "$ZSH_VERSION" ] && ! command -v ghcs &>/dev/null; then
#   # eval "$(gh copilot alias -- zsh)"
# fi
